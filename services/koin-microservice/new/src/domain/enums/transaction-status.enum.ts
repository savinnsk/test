export enum TransactionStatus {
  created = 'created',
  authorized = 'authorized',
  captured = 'captured',
  refunded = 'refunded',
  cancelled = 'cancelled',
  paid = 'paid',
  pending = 'pending',
}
